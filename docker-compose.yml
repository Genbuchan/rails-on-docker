version: '3'
services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -e 'development' -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/workspace
    ports:
      - "3000:3000"
